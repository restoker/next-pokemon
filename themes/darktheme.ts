import { createTheme, NextUIProvider } from "@nextui-org/react"

export const darkTheme = createTheme({
    type: 'dark',
    theme: {
      colors: {
        white: '#ffffff',
        black: '#1A2151',
      },
    }
});